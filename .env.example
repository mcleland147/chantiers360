POSTGRES_DB=chantiers360
POSTGRES_USER=chantiers360
POSTGRES_PASSWORD=chantiers360
POSTGRES_PORT=5432

BACKEND_PORT=3000
FRONTEND_PORT=5173

DATABASE_URL=postgresql://chantiers360:chantiers360@localhost:5432/chantiers360?schema=public
# Obligatoire — min. 32 caractères (démarrage refusé si absent ou faible)
JWT_SECRET=replace-with-strong-secret-at-least-32-chars
JWT_EXPIRES_IN=15m
# Origines CORS (virgules) — ex. https://app.example.com
CORS_ORIGIN=http://localhost:5173

VITE_API_BASE_URL=http://localhost:3000/api
